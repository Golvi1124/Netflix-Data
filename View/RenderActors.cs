using System;

namespace Netflix_Data.View;

public class RenderActors
{

}

/* 
Not yet sure what to do here

Render = something that generates the visual output for the user.
-----------------------------------------------------------------------------------------
public class RenderCustomers
{
    public void DisplayCustomers(List<string> customers)
    {
        customers.ForEach(customer => Console.WriteLine(customer));
    }
}
-----------------------------------------------------------------------------------------
public class RenderMeny
{
    public void DisplayMeny(List<string> meny)
    {
        meny.ForEach(item => Console.WriteLine(item));
    }
    public void DisplayAvailability(string input, bool availability)
    {
        Console.WriteLine($"{input} avaliable? {availability}");
    }
}

 */